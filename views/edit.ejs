<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Thought</title>
  <style>
    /* Reset and basic styling */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #121212;
      color: #f0f0f0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 40px 20px;
    }

    .container {
      background-color: #1f1f1f;
      padding: 30px 40px;
      border-radius: 12px;
      width: 100%;
      max-width: 600px;
      box-shadow: 0 0 15px rgba(255, 193, 7, 0.4);
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
      color: #ffc107;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    textarea {
      resize: vertical;
      min-height: 150px;
      padding: 12px;
      border-radius: 8px;
      border: none;
      font-size: 1.1rem;
      background-color: #2c2c2c;
      color: #f0f0f0;
    }

    input[type="text"] {
      padding: 12px;
      border-radius: 8px;
      border: none;
      font-size: 1.1rem;
      background-color: #2c2c2c;
      color: #f0f0f0;
    }

    button {
      background-color: #ffc107;
      color: #000;
      border: none;
      padding: 15px;
      font-size: 1.2rem;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #e0a800;
    }

    a.back-link {
      display: block;
      text-align: center;
      margin-top: 25px;
      color: #ffc107;
      text-decoration: none;
      font-weight: 600;
    }

    a.back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Edit Thought</h1>
    <form action="/thoughts/<%= thought.id %>?_method=PUT" method="POST">
      <textarea name="content" required><%= thought.content %></textarea>
      <input type="text" name="category" value="<%= thought.category %>" required />
      <button type="submit">Update Thought</button>
    </form>
    <a href="/" class="back-link">Back to List</a>
  </div>
</body>
</html>
